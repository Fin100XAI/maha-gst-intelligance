"""Reference texts the platform is checked against.

Nothing here computes. These are the department's own words, stored verbatim so
that a screen can put them beside what the platform actually does and let a
reader judge the match rather than take it on trust.
"""
